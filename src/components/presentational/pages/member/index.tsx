import { VStack } from '@chakra-ui/react';
import { VFC } from 'react';

import { Member } from '../../member';

const Page: VFC = () => (
  <VStack>
    <Member />
  </VStack>
);

export default Page;
